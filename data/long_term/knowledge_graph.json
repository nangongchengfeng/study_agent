{
  "knowledge_points": {
    "kp-decorator": {
      "name": "Python 装饰器",
      "description": "装饰器是一种设计模式，用于在不修改原函数代码的情况下扩展其功能",
      "category": "python",
      "difficulty": "intermediate",
      "status": "learning",
      "proficiency": 75,
      "tags": [
        "python",
        "decorator",
        "design-pattern"
      ],
      "id": "kp-decorator",
      "created_at": "2026-03-17T17:08:51.720928",
      "updated_at": "2026-03-17T17:08:51.720928"
    },
    "kp-metaclass": {
      "name": "Python 元类",
      "description": "元类是创建类的类，Python中所有类都是type的实例",
      "category": "python",
      "difficulty": "advanced",
      "status": "learning",
      "proficiency": 40,
      "tags": [
        "python",
        "metaclass",
        "advanced"
      ],
      "id": "kp-metaclass",
      "created_at": "2026-03-17T17:08:51.722439",
      "updated_at": "2026-03-17T17:08:51.722439"
    },
    "kp-singleton": {
      "name": "单例模式",
      "description": "确保一个类只有一个实例，并提供全局访问点",
      "category": "design-pattern",
      "difficulty": "beginner",
      "status": "mastered",
      "proficiency": 90,
      "tags": [
        "design-pattern",
        "creational"
      ],
      "id": "kp-singleton",
      "created_at": "2026-03-17T17:08:51.723445",
      "updated_at": "2026-03-17T17:08:51.723445"
    },
    "3e3456382dd2af48a7c438d89d4fc832": {
      "id": "3e3456382dd2af48a7c438d89d4fc832",
      "title": "python???",
      "content": "## 关于 \"python???\" 的讲解\n\n感谢您的提问！这是一个关于编程学习的好问题。\n\n### 学习建议\n\n1. **从基础开始**：确保理解核心概念\n2. **动手实践**：编写代码是最好的学习方式\n3. **阅读文档**：官方文档是最权威的资料\n4. **多做练习**：通过练习巩固知识\n5. **查看优秀代码**：学习他人的编码风格\n\n### 下一步\n\n基于您的问题，我建议：\n- 先了解...",
      "topics": [
        "general"
      ],
      "example": "## 编程学习示例\n\n### 示例 1：基础语法练习\n```python\n# 变量和类型\nname = \"Python\"\nversion = 3.12\nis_awesome = True\n\nprint(f\"{name} {version} 是很棒的: {is_awesome}\")\n\n# 列表操作\nnumbers = [1, 2, 3, 4, 5]\nnumbers.append(6)\nnumbers_double = [x * 2 for x in numbers]\nprint(numbers_double)\n\n# 字典操作\nperson = {\"name\": \"Alice\", \"age\": 25}\nperson[\"city\"] = \"北京\"\nprint(person)\n```\n\n### 示例 2：简单的类\n```python\nclass Animal:\n    def __init__(self, name):\n        self.name = name\n\n    def speak(self):\n        pass\n\nclass Dog(Animal):\n    def speak(self):\n        return f\"{self.name} 说: 汪汪！\"\n\nclass Cat(Animal):\n    def speak(self):\n        return f\"{self.name} 说: 喵喵！\"\n\ndog = Dog(\"旺财\")\ncat = Cat(\"咪咪\")\nprint(dog.speak())\nprint(cat.speak())\n```\n\n### 动手练习\n1. 尝试修改上面的代码\n2. 添加新的功能\n3. 运行看看效果\n\n编程最好的学习方式就是动手写代码！",
      "practice": "## 类和面向对象练习题\n\n### 题目 1：简单的类\n\n**要求**：\n- 创建一个 `Person` 类，有 `name` 和 `age` 属性\n- 有一个 `introduce()` 方法，打印自我介绍\n- 示例：\n```python\nperson = Person(\"Alice\", 25)\nperson.introduce()  # 我叫 Alice，今年 25 岁\n```\n\n---\n\n### 题目 2：继承\n\n**要求**：\n- 创建一个 `Animal` 基类，有 `name` 属性和 `speak()` 方法\n- 创建 `Dog` 和 `Cat` 继承自 `Animal`\n- 重写 `speak()` 方法\n- 示例：\n```python\ndog = Dog(\"旺财\")\ncat = Cat(\"咪咪\")\nprint(dog.speak())  # 汪汪！\nprint(cat.speak())  # 喵喵！\n```\n\n---\n\n### 参考实现\n\n**题目 1**：\n```python\nclass Person:\n    def __init__(self, name, age):\n        self.name = name\n        self.age = age\n\n    def introduce(self):\n        print(f\"我叫 {self.name}，今年 {self.age} 岁\")\n```\n\n**题目 2**：\n```python\nclass Animal:\n    def __init__(self, name):\n        self.name = name\n\n    def speak(self):\n        pass\n\nclass Dog(Animal):\n    def speak(self):\n        return f\"{self.name} 说: 汪汪！\"\n\nclass Cat(Animal):\n    def speak(self):\n        return f\"{self.name} 说: 喵喵！\"\n```",
      "related_knowledge": [],
      "keywords": [
        "Python"
      ],
      "difficulty": "basic",
      "type": "general",
      "user_level": "intermediate",
      "creation_time": "2026-03-17T17:44:42.989108",
      "last_accessed": "2026-03-17T17:44:42.989108",
      "access_count": 1,
      "importance_score": 0.0,
      "mastery_score": 0.5,
      "review_priority": "normal",
      "created_at": "2026-03-17T17:44:41.446405",
      "updated_at": "2026-03-17T17:44:42.990628",
      "status": "learning",
      "proficiency": 0
    },
    "test123": {
      "id": "test123",
      "title": "测试知识",
      "content": "这是测试内容",
      "topics": [
        "测试"
      ],
      "created_at": "2026-03-17T17:45:14.504949",
      "updated_at": "2026-03-17T17:45:14.504949",
      "status": "learning",
      "proficiency": 0
    },
    "33a363f674b92aaab420f3bbc62e9818": {
      "id": "33a363f674b92aaab420f3bbc62e9818",
      "title": "python 是什么",
      "content": "## 关于 \"python 是什么\" 的讲解\n\n感谢您的提问！这是一个关于编程学习的好问题。\n\n### 学习建议\n\n1. **从基础开始**：确保理解核心概念\n2. **动手实践**：编写代码是最好的学习方式\n3. **阅读文档**：官方文档是最权威的资料\n4. **多做练习**：通过练习巩固知识\n5. **查看优秀代码**：学习他人的编码风格\n\n### 下一步\n\n基于您的问题，我建议：\n- 先了...",
      "topics": [
        "general"
      ],
      "example": "## 编程学习示例\n\n### 示例 1：基础语法练习\n```python\n# 变量和类型\nname = \"Python\"\nversion = 3.12\nis_awesome = True\n\nprint(f\"{name} {version} 是很棒的: {is_awesome}\")\n\n# 列表操作\nnumbers = [1, 2, 3, 4, 5]\nnumbers.append(6)\nnumbers_double = [x * 2 for x in numbers]\nprint(numbers_double)\n\n# 字典操作\nperson = {\"name\": \"Alice\", \"age\": 25}\nperson[\"city\"] = \"北京\"\nprint(person)\n```\n\n### 示例 2：简单的类\n```python\nclass Animal:\n    def __init__(self, name):\n        self.name = name\n\n    def speak(self):\n        pass\n\nclass Dog(Animal):\n    def speak(self):\n        return f\"{self.name} 说: 汪汪！\"\n\nclass Cat(Animal):\n    def speak(self):\n        return f\"{self.name} 说: 喵喵！\"\n\ndog = Dog(\"旺财\")\ncat = Cat(\"咪咪\")\nprint(dog.speak())\nprint(cat.speak())\n```\n\n### 动手练习\n1. 尝试修改上面的代码\n2. 添加新的功能\n3. 运行看看效果\n\n编程最好的学习方式就是动手写代码！",
      "practice": "## 类和面向对象练习题\n\n### 题目 1：简单的类\n\n**要求**：\n- 创建一个 `Person` 类，有 `name` 和 `age` 属性\n- 有一个 `introduce()` 方法，打印自我介绍\n- 示例：\n```python\nperson = Person(\"Alice\", 25)\nperson.introduce()  # 我叫 Alice，今年 25 岁\n```\n\n---\n\n### 题目 2：继承\n\n**要求**：\n- 创建一个 `Animal` 基类，有 `name` 属性和 `speak()` 方法\n- 创建 `Dog` 和 `Cat` 继承自 `Animal`\n- 重写 `speak()` 方法\n- 示例：\n```python\ndog = Dog(\"旺财\")\ncat = Cat(\"咪咪\")\nprint(dog.speak())  # 汪汪！\nprint(cat.speak())  # 喵喵！\n```\n\n---\n\n### 参考实现\n\n**题目 1**：\n```python\nclass Person:\n    def __init__(self, name, age):\n        self.name = name\n        self.age = age\n\n    def introduce(self):\n        print(f\"我叫 {self.name}，今年 {self.age} 岁\")\n```\n\n**题目 2**：\n```python\nclass Animal:\n    def __init__(self, name):\n        self.name = name\n\n    def speak(self):\n        pass\n\nclass Dog(Animal):\n    def speak(self):\n        return f\"{self.name} 说: 汪汪！\"\n\nclass Cat(Animal):\n    def speak(self):\n        return f\"{self.name} 说: 喵喵！\"\n```",
      "related_knowledge": [],
      "keywords": [
        "Python"
      ],
      "difficulty": "basic",
      "type": "general",
      "user_level": "intermediate",
      "creation_time": "2026-03-17T17:45:30.910241",
      "last_accessed": "2026-03-17T17:45:30.910241",
      "access_count": 1,
      "importance_score": 0.0,
      "mastery_score": 0.5,
      "review_priority": "normal",
      "created_at": "2026-03-17T17:45:30.425468",
      "updated_at": "2026-03-17T17:45:30.910241",
      "status": "learning",
      "proficiency": 0
    },
    "bcd2a652c45deed9713cff35dfaddefa": {
      "id": "bcd2a652c45deed9713cff35dfaddefa",
      "title": "python的封装，继承，多态 是什么",
      "content": "## 关于 \"python的封装，继承，多态 是什么\" 的讲解\n\n感谢您的提问！这是一个关于编程学习的好问题。\n\n### 学习建议\n\n1. **从基础开始**：确保理解核心概念\n2. **动手实践**：编写代码是最好的学习方式\n3. **阅读文档**：官方文档是最权威的资料\n4. **多做练习**：通过练习巩固知识\n5. **查看优秀代码**：学习他人的编码风格\n\n### 下一步\n\n基于您的问题，...",
      "topics": [
        "general"
      ],
      "example": "## 编程学习示例\n\n### 示例 1：基础语法练习\n```python\n# 变量和类型\nname = \"Python\"\nversion = 3.12\nis_awesome = True\n\nprint(f\"{name} {version} 是很棒的: {is_awesome}\")\n\n# 列表操作\nnumbers = [1, 2, 3, 4, 5]\nnumbers.append(6)\nnumbers_double = [x * 2 for x in numbers]\nprint(numbers_double)\n\n# 字典操作\nperson = {\"name\": \"Alice\", \"age\": 25}\nperson[\"city\"] = \"北京\"\nprint(person)\n```\n\n### 示例 2：简单的类\n```python\nclass Animal:\n    def __init__(self, name):\n        self.name = name\n\n    def speak(self):\n        pass\n\nclass Dog(Animal):\n    def speak(self):\n        return f\"{self.name} 说: 汪汪！\"\n\nclass Cat(Animal):\n    def speak(self):\n        return f\"{self.name} 说: 喵喵！\"\n\ndog = Dog(\"旺财\")\ncat = Cat(\"咪咪\")\nprint(dog.speak())\nprint(cat.speak())\n```\n\n### 动手练习\n1. 尝试修改上面的代码\n2. 添加新的功能\n3. 运行看看效果\n\n编程最好的学习方式就是动手写代码！",
      "practice": "## 类和面向对象练习题\n\n### 题目 1：简单的类\n\n**要求**：\n- 创建一个 `Person` 类，有 `name` 和 `age` 属性\n- 有一个 `introduce()` 方法，打印自我介绍\n- 示例：\n```python\nperson = Person(\"Alice\", 25)\nperson.introduce()  # 我叫 Alice，今年 25 岁\n```\n\n---\n\n### 题目 2：继承\n\n**要求**：\n- 创建一个 `Animal` 基类，有 `name` 属性和 `speak()` 方法\n- 创建 `Dog` 和 `Cat` 继承自 `Animal`\n- 重写 `speak()` 方法\n- 示例：\n```python\ndog = Dog(\"旺财\")\ncat = Cat(\"咪咪\")\nprint(dog.speak())  # 汪汪！\nprint(cat.speak())  # 喵喵！\n```\n\n---\n\n### 参考实现\n\n**题目 1**：\n```python\nclass Person:\n    def __init__(self, name, age):\n        self.name = name\n        self.age = age\n\n    def introduce(self):\n        print(f\"我叫 {self.name}，今年 {self.age} 岁\")\n```\n\n**题目 2**：\n```python\nclass Animal:\n    def __init__(self, name):\n        self.name = name\n\n    def speak(self):\n        pass\n\nclass Dog(Animal):\n    def speak(self):\n        return f\"{self.name} 说: 汪汪！\"\n\nclass Cat(Animal):\n    def speak(self):\n        return f\"{self.name} 说: 喵喵！\"\n```",
      "related_knowledge": [],
      "keywords": [
        "Python"
      ],
      "difficulty": "basic",
      "type": "general",
      "user_level": "intermediate",
      "creation_time": "2026-03-17T18:05:02.011552",
      "last_accessed": "2026-03-17T18:05:02.011552",
      "access_count": 1,
      "importance_score": 0.0,
      "mastery_score": 0.5,
      "review_priority": "normal",
      "created_at": "2026-03-17T17:49:25.082265",
      "updated_at": "2026-03-17T18:05:02.012075",
      "status": "learning",
      "proficiency": 0
    },
    "e58565d69d5fed86340f19707d75d626": {
      "id": "e58565d69d5fed86340f19707d75d626",
      "title": "python怎么实现文件读写",
      "content": "## 关于 \"python怎么实现文件读写\" 的讲解\n\n感谢您的提问！这是一个关于编程学习的好问题。\n\n### 学习建议\n\n1. **从基础开始**：确保理解核心概念\n2. **动手实践**：编写代码是最好的学习方式\n3. **阅读文档**：官方文档是最权威的资料\n4. **多做练习**：通过练习巩固知识\n5. **查看优秀代码**：学习他人的编码风格\n\n### 下一步\n\n基于您的问题，我建议：...",
      "topics": [
        "general"
      ],
      "example": "## 编程学习示例\n\n### 示例 1：基础语法练习\n```python\n# 变量和类型\nname = \"Python\"\nversion = 3.12\nis_awesome = True\n\nprint(f\"{name} {version} 是很棒的: {is_awesome}\")\n\n# 列表操作\nnumbers = [1, 2, 3, 4, 5]\nnumbers.append(6)\nnumbers_double = [x * 2 for x in numbers]\nprint(numbers_double)\n\n# 字典操作\nperson = {\"name\": \"Alice\", \"age\": 25}\nperson[\"city\"] = \"北京\"\nprint(person)\n```\n\n### 示例 2：简单的类\n```python\nclass Animal:\n    def __init__(self, name):\n        self.name = name\n\n    def speak(self):\n        pass\n\nclass Dog(Animal):\n    def speak(self):\n        return f\"{self.name} 说: 汪汪！\"\n\nclass Cat(Animal):\n    def speak(self):\n        return f\"{self.name} 说: 喵喵！\"\n\ndog = Dog(\"旺财\")\ncat = Cat(\"咪咪\")\nprint(dog.speak())\nprint(cat.speak())\n```\n\n### 动手练习\n1. 尝试修改上面的代码\n2. 添加新的功能\n3. 运行看看效果\n\n编程最好的学习方式就是动手写代码！",
      "practice": "## 类和面向对象练习题\n\n### 题目 1：简单的类\n\n**要求**：\n- 创建一个 `Person` 类，有 `name` 和 `age` 属性\n- 有一个 `introduce()` 方法，打印自我介绍\n- 示例：\n```python\nperson = Person(\"Alice\", 25)\nperson.introduce()  # 我叫 Alice，今年 25 岁\n```\n\n---\n\n### 题目 2：继承\n\n**要求**：\n- 创建一个 `Animal` 基类，有 `name` 属性和 `speak()` 方法\n- 创建 `Dog` 和 `Cat` 继承自 `Animal`\n- 重写 `speak()` 方法\n- 示例：\n```python\ndog = Dog(\"旺财\")\ncat = Cat(\"咪咪\")\nprint(dog.speak())  # 汪汪！\nprint(cat.speak())  # 喵喵！\n```\n\n---\n\n### 参考实现\n\n**题目 1**：\n```python\nclass Person:\n    def __init__(self, name, age):\n        self.name = name\n        self.age = age\n\n    def introduce(self):\n        print(f\"我叫 {self.name}，今年 {self.age} 岁\")\n```\n\n**题目 2**：\n```python\nclass Animal:\n    def __init__(self, name):\n        self.name = name\n\n    def speak(self):\n        pass\n\nclass Dog(Animal):\n    def speak(self):\n        return f\"{self.name} 说: 汪汪！\"\n\nclass Cat(Animal):\n    def speak(self):\n        return f\"{self.name} 说: 喵喵！\"\n```",
      "related_knowledge": [],
      "keywords": [
        "Python"
      ],
      "difficulty": "basic",
      "type": "how_to",
      "user_level": "intermediate",
      "creation_time": "2026-03-17T18:05:28.511345",
      "last_accessed": "2026-03-17T18:05:28.511345",
      "access_count": 1,
      "importance_score": 0.0,
      "mastery_score": 0.5,
      "review_priority": "normal",
      "created_at": "2026-03-17T18:05:28.504696",
      "updated_at": "2026-03-17T18:05:28.512192",
      "status": "learning",
      "proficiency": 0
    },
    "7eca689f0d3389d9dea66ae112e5cfd7": {
      "id": "7eca689f0d3389d9dea66ae112e5cfd7",
      "title": "你好",
      "content": "## 关于 \"你好\" 的讲解\n\n感谢您的提问！这是一个关于编程学习的好问题。\n\n### 学习建议\n\n1. **从基础开始**：确保理解核心概念\n2. **动手实践**：编写代码是最好的学习方式\n3. **阅读文档**：官方文档是最权威的资料\n4. **多做练习**：通过练习巩固知识\n5. **查看优秀代码**：学习他人的编码风格\n\n### 下一步\n\n基于您的问题，我建议：\n- 先了解基本概念\n-...",
      "topics": [
        "general"
      ],
      "example": "## 编程学习示例\n\n### 示例 1：基础语法练习\n```python\n# 变量和类型\nname = \"Python\"\nversion = 3.12\nis_awesome = True\n\nprint(f\"{name} {version} 是很棒的: {is_awesome}\")\n\n# 列表操作\nnumbers = [1, 2, 3, 4, 5]\nnumbers.append(6)\nnumbers_double = [x * 2 for x in numbers]\nprint(numbers_double)\n\n# 字典操作\nperson = {\"name\": \"Alice\", \"age\": 25}\nperson[\"city\"] = \"北京\"\nprint(person)\n```\n\n### 示例 2：简单的类\n```python\nclass Animal:\n    def __init__(self, name):\n        self.name = name\n\n    def speak(self):\n        pass\n\nclass Dog(Animal):\n    def speak(self):\n        return f\"{self.name} 说: 汪汪！\"\n\nclass Cat(Animal):\n    def speak(self):\n        return f\"{self.name} 说: 喵喵！\"\n\ndog = Dog(\"旺财\")\ncat = Cat(\"咪咪\")\nprint(dog.speak())\nprint(cat.speak())\n```\n\n### 动手练习\n1. 尝试修改上面的代码\n2. 添加新的功能\n3. 运行看看效果\n\n编程最好的学习方式就是动手写代码！",
      "practice": "## 类和面向对象练习题\n\n### 题目 1：简单的类\n\n**要求**：\n- 创建一个 `Person` 类，有 `name` 和 `age` 属性\n- 有一个 `introduce()` 方法，打印自我介绍\n- 示例：\n```python\nperson = Person(\"Alice\", 25)\nperson.introduce()  # 我叫 Alice，今年 25 岁\n```\n\n---\n\n### 题目 2：继承\n\n**要求**：\n- 创建一个 `Animal` 基类，有 `name` 属性和 `speak()` 方法\n- 创建 `Dog` 和 `Cat` 继承自 `Animal`\n- 重写 `speak()` 方法\n- 示例：\n```python\ndog = Dog(\"旺财\")\ncat = Cat(\"咪咪\")\nprint(dog.speak())  # 汪汪！\nprint(cat.speak())  # 喵喵！\n```\n\n---\n\n### 参考实现\n\n**题目 1**：\n```python\nclass Person:\n    def __init__(self, name, age):\n        self.name = name\n        self.age = age\n\n    def introduce(self):\n        print(f\"我叫 {self.name}，今年 {self.age} 岁\")\n```\n\n**题目 2**：\n```python\nclass Animal:\n    def __init__(self, name):\n        self.name = name\n\n    def speak(self):\n        pass\n\nclass Dog(Animal):\n    def speak(self):\n        return f\"{self.name} 说: 汪汪！\"\n\nclass Cat(Animal):\n    def speak(self):\n        return f\"{self.name} 说: 喵喵！\"\n```",
      "related_knowledge": [],
      "keywords": [],
      "difficulty": "basic",
      "type": "general",
      "user_level": "intermediate",
      "creation_time": "2026-03-17T18:05:59.525677",
      "last_accessed": "2026-03-17T18:05:59.525677",
      "access_count": 1,
      "importance_score": 0.0,
      "mastery_score": 0.5,
      "review_priority": "normal",
      "created_at": "2026-03-17T18:05:59.520767",
      "updated_at": "2026-03-17T18:05:59.527189",
      "status": "learning",
      "proficiency": 0
    },
    "6ca59955f02e32e8a6290e946e769c2f": {
      "id": "6ca59955f02e32e8a6290e946e769c2f",
      "title": "python的继承是什么",
      "content": "# Python继承：让代码“继承家业”的智慧\n\n## 一、生活化类比：家族企业的传承\n\n想象一下你家里开了一家面包店，你爸爸是老板，店里有一套成熟的经营方法：\n- 招牌配方：独家面包配方\n- 经营流程：早上6点开门，晚上8点关门\n- 服务标准：微笑服务，买三送一\n\n现在你要开一家分店，你有两个选择：\n1. **从头开始**：重新研究配方、制定流程、设计服务标准（很累，容易出错）\n2. **继承家...",
      "topics": [
        "类",
        "列表"
      ],
      "example": "```python\n\"\"\"\n示例1：基础继承 - 面包店家族\n演示内容：最基本的继承语法，子类如何继承父类的属性和方法\n适合水平：intermediate\n\"\"\"\n\n# 父类：基础面包店\nclass BaseBakery:\n    \"\"\"基础面包店类，包含面包店的基本功能\"\"\"\n    \n    def __init__(self, name):\n        \"\"\"初始化面包店\"\"\"\n        self.name = name  # 店名\n        self.recipe = \"经典面包配方\"  # 招牌配方\n        self.hours = \"6:00-20:00\"  # 营业时间\n    \n    def display_info(self):\n        \"\"\"显示面包店信息\"\"\"\n        return f\"{self.name}面包店 | 配方: {self.recipe} | 营业: {self.hours}\"\n    \n    def make_bread(self, bread_type):\n        \"\"\"制作面包\"\"\"\n        return f\"制作{self.recipe}的{bread_type}面包\"\n\n# 子类：特色面包店（继承BaseBakery）\nclass SpecialBakery(BaseBakery):\n    \"\"\"特色面包店，继承基础面包店并添加新功能\"\"\"\n    \n    def __init__(self, name, special_recipe):\n        \"\"\"初始化特色面包店\"\"\"\n        # 使用super()调用父类的__init__方法\n        super().__init__(name)\n        self.special_recipe = special_recipe  # 添加特色配方\n        self.delivery_service = True  # 新增配送服务\n    \n    # 重写父类方法：修改营业时间\n    def display_info(self):\n        \"\"\"重写显示信息，添加特色内容\"\"\"\n        base_info = super().display_info()  # 先获取父类信息\n        return f\"{base_info} | 特色: {self.special_recipe} | 配送: {'支持' if self.delivery_service else '不支持'}\"\n    \n    # 新增方法：父类没有的功能\n    def make_special_bread(self):\n        \"\"\"制作特色面包（子类特有方法）\"\"\"\n        return f\"制作特色{self.special_recipe}面包\"\n    \n    def offer_delivery(self, address):\n        \"\"\"提供配送服务（子类特有方法）\"\"\"\n        if self.delivery_service:\n            return f\"配送至: {address}\"\n        return \"暂不提供配送\"\n\n# 使用示例\nprint(\"=== 示例1：基础继承演示 ===\")\nprint()\n\n# 创建父类实例\nbase_shop = BaseBakery(\"老王家\")\nprint(\"1. 父类面包店:\")\nprint(f\"   {base_shop.display_info()}\")\nprint(f\"   {base_shop.make_bread('法棍')}\")\nprint()\n\n# 创建子类实例\nspecial_shop = SpecialBakery(\"创意坊\", \"全麦坚果\")\nprint(\"2. 子类面包店（继承父类）:\")\nprint(f\"   {special_shop.display_info()}\")  # 调用重写的方法\nprint(f\"   {special_shop.make_bread('吐司')}\")  # 调用继承的方法\nprint(f\"   {special_shop.make_special_bread()}\")  # 调用子类特有方法\nprint(f\"   {special_shop.offer_delivery('北京路123号')}\")  # 调用子类特有方法\nprint()\n\n# 检查继承关系\nprint(\"3. 继承关系检查:\")\nprint(f\"   special_shop是BaseBakery的实例吗？ {isinstance(special_shop, BaseBakery)}\")\nprint(f\"   SpecialBakery是BaseBakery的子类吗？ {issubclass(SpecialBakery, BaseBakery)}\")\nprint(f\"   SpecialBakery的父类: {SpecialBakery.__bases__}\")\n```\n\n```python\n\"\"\"\n示例2：方法重写与扩展 - 电商产品系统\n演示内容：如何重写父类方法，以及使用super()扩展父类功能\n适合水平：intermediate\n\"\"\"\n\n# 父类：基础产品\nclass Product:\n    \"\"\"基础产品类，定义产品的通用属性和方法\"\"\"\n    \n    def __init__(self, name, price):\n        \"\"\"初始化产品\"\"\"\n        self.name = name\n        self.price = price\n        self.category = \"通用商品\"\n    \n    def get_description(self):\n        \"\"\"获取产品描述\"\"\"\n        return f\"{self.name} - ${self.price}\"\n    \n    def calculate_price(self, quantity=1):\n        \"\"\"计算总价\"\"\"\n        return self.price * quantity\n    \n    def apply_discount(self, discount_percent):\n        \"\"\"应用折扣\"\"\"\n        if 0 <= discount_percent <= 100:\n            discounted_price = self.price * (1 - discount_percent / 100)\n            return round(discounted_price, 2)\n        return self.price\n\n# 子类1：书籍产品（完全重写折扣方法）\nclass Book(Product):\n    \"\"\"书籍产品，继承Product但有自己的折扣规则\"\"\"\n    \n    def __init__(self, name, price, author, pages):\n        \"\"\"初始化书籍\"\"\"\n        super().__init__(name, price)\n        self.author = author\n        self.pages = pages\n        self.category = \"图书\"\n    \n    # 完全重写：书籍最多打8折\n    def apply_discount(self, discount_percent):\n        \"\"\"重写折扣方法，限制最大折扣\"\"\"\n        if discount_percent > 20:  # 书籍最多打8折\n            discount_percent = 20\n        return super().apply_discount(discount_percent)\n    \n    # 扩展父类方法\n    def get_description(self):\n        \"\"\"扩展描述信息，添加书籍详情\"\"\"\n        base_desc = super().get_description()  # 先获取基础描述\n        return f\"{base_desc} | 作者: {self.author} | 页数: {self.pages}页\"\n\n# 子类2：电子产品（扩展父类方法）\nclass Electronics(Product):\n    \"\"\"电子产品，继承Product并添加保修信息\"\"\"\n    \n    def __init__(self, name, price, brand, warranty_months):\n        \"\"\"初始化电子产品\"\"\"\n        super().__init__(name, price)\n        self.brand = brand\n        self.warranty_months = warranty_months\n        self.category = \"电子产品\"\n    \n    # 扩展父类方法：添加保修信息\n    def get_description(self):\n        \"\"\"扩展描述信息，添加品牌和保修\"\"\"\n        base_desc = super().get_description()\n        return f\"{base_desc} | 品牌: {self.brand} | 保修: {self.warranty_months}个月\"\n    \n    # 新增方法：电子产品特有\n    def check_warranty(self, purchase_month):\n        \"\"\"检查保修状态\"\"\"\n        current_month = 12  # 假设当前是12月\n        if current_month - purchase_month <= self.warranty_months:\n            return f\"在保修期内（剩余{self.warranty_months - (current_month - purchase_month)}个月）\"\n        return \"已过保修期\"\n\n# 使用示例\nprint(\"=== 示例2：方法重写与扩展 ===\")\nprint()\n\n# 创建基础产品\nprint(\"1. 基础产品:\")\nbasic_product = Product(\"通用商品\", 100)\nprint(f\"   描述: {basic_product.get_description()}\")\nprint(f\"   原价: ${basic_product.price}\")\nprint(f\"   8折后: ${basic_product.apply_discount(20)}\")\nprint(f\"   5折后: ${basic_product.apply_discount(50)}\")\nprint()\n\n# 创建书籍产品\nprint(\"2. 书籍产品（重写折扣方法）:\")\nbook = Book(\"Python编程入门\", 80, \"张三\", 300)\nprint(f\"   描述: {book.get_description()}\")\nprint(f\"   原价: ${book.price}\")\nprint(f\"   8折后: ${book.apply_discount(20)}\")  # 正常折扣\nprint(f\"   尝试5折: ${book.apply_discount(50)}\")  # 被限制为最大8折\nprint()\n\n# 创建电子产品\nprint(\"3. 电子产品（扩展父类方法）:\")\nlaptop = Electronics(\"笔记本电脑\", 1200, \"Dell\", 24)\nprint(f\"   描述: {laptop.get_description()}\")\nprint(f\"   原价: ${laptop.price}\")\nprint(f\"   9折后: ${laptop.apply_discount(10)}\")\nprint(f\"   保修检查: {laptop.check_warranty(6)}\")  # 6月购买\nprint()\n\n# 多态性演示\nprint(\"4. 多态性演示（统一接口，不同实现）:\")\nproducts = [basic_product, book, laptop]\n\nfor i, product in enumerate(products, 1):\n    print(f\"   产品{i} - {product.category}:\")\n    print(f\"     描述: {product.get_description()}\")\n    print(f\"     7折价: ${product.apply_discount(30)}\")\n    print()\n```\n\n```python\n\"\"\"\n示例3：多重继承与MRO - 智能设备系统\n演示内容：多重继承的使用、方法解析顺序(MRO)和混入类(Mixin)\n适合水平：intermediate\n\"\"\"\n\n# 基础类1：联网功能\nclass NetworkDevice:\n    \"\"\"提供网络连接功能\"\"\"\n    \n    def __init__(self):\n        self.connected = False\n        self.ip_address = None\n    \n    def connect(self, ip):\n        \"\"\"连接到网络\"\"\"\n        self.ip_address = ip\n        self.connected = True\n        return f\"已连接到 {ip}\"\n    \n    def disconnect(self):\n        \"\"\"断开网络连接\"\"\"\n        self.connected = False\n        self.ip_address = None\n        return \"已断开连接\"\n    \n    def get_network_status(self):\n        \"\"\"获取网络状态\"\"\"\n        status = \"在线\" if self.connected else \"离线\"\n        return f\"网络状态: {status} | IP: {self.ip_address or '未分配'}\"\n\n# 基础类2：电源管理\nclass PowerDevice:\n    \"\"\"提供电源管理功能\"\"\"\n    \n    def __init__(self):\n        self.powered_on = False\n        self.battery_level = 100\n    \n    def power_on(self):\n        \"\"\"开机\"\"\"\n        self.powered_on = True\n        return \"设备已开机\"\n    \n    def power_off(self):\n        \"\"\"关机\"\"\"\n        self.powered_on = False\n        return \"设备已关机\"\n    \n    def check_battery(self):\n        \"\"\"检查电量\"\"\"\n        return f\"电量: {self.battery_level}%\"\n    \n    def charge(self, amount):\n        \"\"\"充电\"\"\"\n        self.battery_level = min(100, self.battery_level + amount)\n        return f\"已充电{amount}%，当前电量: {self.battery_level}%\"\n\n# Mixin类：日志功能（不单独使用，只用于混入）\nclass LoggingMixin:\n    \"\"\"日志混入类，为设备添加日志功能\"\"\"\n    \n    def log(self, message):\n        \"\"\"记录日志\"\"\"\n        import datetime\n        timestamp = datetime.datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")\n        print(f\"[{timestamp}] [{self.__class__.__name__}] {message}\")\n    \n    def log_operation(self, operation):\n        \"\"\"记录操作日志\"\"\"\n        self.log(f\"执行操作: {operation}\")\n\n# 多重继承：智能设备继承多个父类\nclass SmartDevice(NetworkDevice, PowerDevice, LoggingMixin):\n    \"\"\"智能设备，继承网络功能、电源管理和日志功能\"\"\"\n    \n    def __init__(self, name):\n        \"\"\"初始化智能设备\"\"\"\n        # 分别调用父类的初始化方法\n        NetworkDevice.__init__(self)\n        PowerDevice.__init__(self)\n        self.name = name\n        self.log(f\"设备 '{name}' 初始化完成\")\n    \n    def full_startup(self):\n        \"\"\"完整启动流程\"\"\"\n        self.log_operation(\"完整启动\")\n        \n        # 开机\n        result1 = self.power_on()\n        self.log(result1)\n        \n        # 连接网络\n        result2 = self.connect(\"192.168.1.100\")\n        self.log(result2)\n        \n        return f\"{result1}，{result2}\"\n    \n    def get_status(self):\n        \"\"\"获取设备完整状态\"\"\"\n        network_status = self.get_network_status()\n        power_status = self.check_battery()\n        device_status = \"运行中\" if self.powered_on else \"已关机\"\n        \n        return f\"\"\"\n设备名称: {self.name}\n设备状态: {device_status}\n{network_status}\n{power_status}\n        \"\"\".strip()\n\n# 使用示例\nprint(\"=== 示例3：多重继承与MRO ===\")\nprint()\n\n# 创建智能设备\nprint(\"1. 创建智能设备:\")\nsmart_home = SmartDevice(\"智能家居中心\")\nprint(f\"   设备创建成功: {smart_home.name}\")\nprint()\n\n# 演示多重继承的功能\nprint(\"2. 演示继承的功能:\")\nprint(f\"   a) 网络功能: {smart_home.connect('192.168.1.101')}\")\nprint(f\"   b) 电源功能: {smart_home.power_on()}\")\nprint(f\"   c) 日志功能: \", end=\"\")\nsmart_home.log(\"测试日志消息\")\nprint()\n\n# 完整状态检查\nprint(\"3. 设备完整状态:\")\nprint(smart_home.get_status())\nprint()\n\n# 演示方法解析顺序(MRO)\nprint(\"4. 方法解析顺序(MRO):\")\nprint(\"   SmartDevice的MRO顺序:\")\nfor i, cls in enumerate(SmartDevice.__mro__, 1):\n    print(f\"   {i}. {cls.__name__}\")\nprint()\n\n# 演示混入类的使用\nprint(\"5. 使用混入类的日志功能:\")\nsmart_home.full_startup()\nprint()\n\n# 充电演示\nprint(\"6. 电源管理功能:\")\nprint(f\"   {smart_home.check_battery()}\")\nprint(f\"   {smart_home.charge(30)}\")\nprint(f\"   {smart_home.charge(50)}\")  # 不会超过100%\nprint()\n\n# 断开连接\nprint(\"7. 清理操作:\")\nprint(f\"   {smart_home.disconnect()}\")\nprint(f\"   {smart_home.power_off()}\")\nsmart_home.log(\"设备已关闭\")\n```\n\n```python\n\"\"\"\n示例4：抽象基类与接口设计 - 图形计算系统\n演示内容：使用抽象基类定义接口，强制子类实现特定方法\n适合水平：intermediate\n\"\"\"\n\nfrom abc import ABC, abstractmethod\nimport math\n\n# 抽象基类：图形接口\nclass Shape(ABC):\n    \"\"\"图形抽象基类，定义所有图形必须实现的方法\"\"\"\n    \n    def __init__(self, name):\n        self.name = name\n    \n    @abstractmethod\n    def area(self):\n        \"\"\"计算面积（抽象方法，子类必须实现）\"\"\"\n        pass\n    \n    @abstractmethod\n    def perimeter(self):\n        \"\"\"计算周长（抽象方法，子类必须实现）\"\"\"\n        pass\n    \n    def display_info(self):\n        \"\"\"显示图形信息（具体方法，子类可以直接使用）\"\"\"\n        return f\"图形: {self.name} | 面积: {self.area():.2f} | 周长: {self.perimeter():.2f}\"\n    \n    # 类方法：工厂方法模式\n    @classmethod\n    def create_from_dict(cls, shape_dict):\n        \"\"\"从字典创建图形（类方法示例）\"\"\"\n        shape_type = shape_dict.get('type')\n        if shape_type == 'circle':\n            return Circle(shape_dict['radius'])\n        elif shape_type == 'rectangle':\n            return Rectangle(shape_dict['width'], shape_dict['height'])\n        elif shape_type == 'triangle':\n            return Triangle(*shape_dict['sides'])\n        else:\n            raise ValueError(f\"不支持的图形类型: {shape_type}\")\n\n# 具体子类1：圆形\nclass Circle(Shape):\n    \"\"\"圆形，实现Shape抽象基类\"\"\"\n    \n    def __init__(self, radius):\n        super().__init__(\"圆形\")\n        self.radius = radius\n    \n    def area(self):\n        \"\"\"计算圆形面积\"\"\"\n        return math.pi * self.radius ** 2\n    \n    def perimeter(self):\n        \"\"\"计算圆形周长\"\"\"\n        return 2 * math.pi * self.radius\n    \n    # 添加圆形特有的方法\n    def diameter(self):\n        \"\"\"计算直径\"\"\"\n        return 2 * self.radius\n\n# 具体子类2：矩形\nclass Rectangle(Shape):\n    \"\"\"矩形，实现Shape抽象基类\"\"\"\n    \n    def __init__(self, width, height):\n        super().__init__(\"矩形\")\n        self.width = width\n        self.height = height\n    \n    def area(self):\n        \"\"\"计算矩形面积\"\"\"\n        return self.width * self.height\n    \n    def perimeter(self):\n        \"\"\"计算矩形周长\"\"\"\n        return 2 * (self.width + self.height)\n    \n    # 添加矩形特有的方法\n    def is_square(self):\n        \"\"\"判断是否是正方形\"\"\"\n        return self.width == self.height\n\n# 具体子类3：三角形\nclass Triangle(Shape):\n    \"\"\"三角形，实现Shape抽象基类\"\"\"\n    \n    def __init__(self, a, b, c):\n        super().__init__(\"三角形\")\n        self.sides = (a, b, c)\n    \n    def area(self):\n        \"\"\"使用海伦公式计算三角形面积\"\"\"\n        a, b, c = self.sides\n        s = self.perimeter() / 2  # 半周长\n        return math.sqrt(s * (s - a) * (s - b) * (s - c))\n    \n    def perimeter(self):\n        \"\"\"计算三角形周长\"\"\"\n        return sum(self.sides)\n    \n    # 添加三角形特有的方法\n    def triangle_type(self):\n        \"\"\"判断三角形类型\"\"\"\n        a, b, c = sorted(self.sides)\n        if a == b == c:\n            return \"等边三角形\"\n        elif a == b or b == c or a == c:\n            return \"等腰三角形\"\n        elif a**2 + b**2 == c**2:\n            return \"直角三角形\"\n        else:\n            return \"一般三角形\"\n\n# 使用示例\nprint(\"=== 示例4：抽象基类与接口设计 ===\")\nprint()\n\n# 创建各种图形\nprint(\"1. 创建各种图形并计算:\")\nshapes = [\n    Circle(5),\n    Rectangle(4, 6),\n    Rectangle(5, 5),  # 正方形\n    Triangle(3, 4, 5),\n    Triangle(5, 5, 5),  # 等边三角形\n]\n\nfor shape in shapes:\n    print(f\"   {shape.display_info()}\")\n    \n    # 调用特有方法\n    if isinstance(shape, Circle):\n        print(f\"     直径: {shape.diameter():.2f}\")\n    elif isinstance(shape, Rectangle):\n        square_status = \"是\" if shape.is_square() else \"不是\"\n        print(f\"     {square_status}正方形\")\n    elif isinstance(shape, Triangle):\n        print(f\"     类型: {shape.triangle_type()}\")\n    print()\n\n# 演示抽象基类的约束\nprint(\"2. 抽象基类约束演示:\")\ntry:\n    # 尝试创建没有实现抽象方法的类\n    class InvalidShape(Shape):\n        \"\"\"没有实现抽象方法的无效类\"\"\"\n        pass\n    \n    invalid = InvalidShape(\"无效图形\")  # 这里会报错\nexcept TypeError as e:\n    print(f\"   错误: {e}\")\n    print(\"   ✓ 抽象基类成功阻止了不完整的实现\")\nprint()\n\n# 使用工厂方法创建图形\nprint(\"3. 工厂方法模式演示:\")\nshape_data = [\n    {'type': 'circle', 'radius': 3},\n    {'type': 'rectangle', 'width': 4, 'height': 7},\n    {'type': 'triangle', 'sides': [6, 8, 10]},\n]\n\nprint(\"   从字典数据创建图形:\")\nfor data in shape_data:\n    shape = Shape.create_from_dict(data)\n    print(f\"   {shape.display_info()}\")\nprint()\n\n# 多态性：统一处理不同图形\nprint(\"4. 多态性应用：计算总面积和总周长:\")\ntotal_area = sum(shape.area() for shape in shapes)\ntotal_perimeter = sum(shape.perimeter() for shape in shapes)\nprint(f\"   所有图形的总面积: {total_area:.2f}\")\nprint(f\"   所有图形的总周长: {total_perimeter:.2f}\")\nprint()\n\n# 类型检查\nprint(\"5. 类型检查:\")\ncircle = Circle(10)\nprint(f\"   circle是Shape的实例吗？ {isinstance(circle, Shape)}\")\nprint(f\"   circle是Circle的实例吗？ {isinstance(circle, Circle)}\")\nprint(f\"   Circle是Shape的子类吗？ {issubclass(Circle, Shape)}\")\n```",
      "practice": "# Python继承练习题\n\n## 题目1：基础继承实现\n**难度：** ⭐⭐\n**要求：** 创建一个`Vehicle`（交通工具）父类，包含`brand`、`color`属性和`start_engine()`、`stop_engine()`方法。然后创建`Car`（汽车）子类继承`Vehicle`，添加`doors`属性，并重写`start_engine()`方法使其返回特定信息。\n\n**提示：**\n- 使用`super()`调用父类方法\n- 子类的`__init__`方法需要先初始化父类属性\n- 重写方法时注意保持功能一致性\n\n**参考答案：**\n```python\nclass Vehicle:\n    def __init__(self, brand, color):\n        self.brand = brand\n        self.color = color\n        self.engine_on = False\n    \n    def start_engine(self):\n        self.engine_on = True\n        return f\"{self.brand} 引擎启动\"\n    \n    def stop_engine(self):\n        self.engine_on = False\n        return f\"{self.brand} 引擎停止\"\n\nclass Car(Vehicle):\n    def __init__(self, brand, color, doors):\n        super().__init__(brand, color)\n        self.doors = doors\n    \n    def start_engine(self):\n        self.engine_on = True\n        return f\"{self.brand}汽车（{self.doors}门）引擎启动，准备出发！\"\n    \n    def display_info(self):\n        return f\"{self.color}色 {self.brand}，{self.doors}门\"\n\n# 测试\nmy_car = Car(\"Toyota\", \"红\", 4)\nprint(my_car.start_engine())\nprint(my_car.display_info())\nprint(my_car.stop_engine())\n```\n\n---\n\n## 题目2：方法重写与扩展\n**难度：** ⭐⭐⭐\n**要求：** 创建一个`Employee`（员工）父类，包含`name`、`salary`属性和`calculate_bonus()`方法（奖金为工资的10%）。创建`Manager`（经理）子类，重写`calculate_bonus()`方法（奖金为工资的20% + 固定津贴1000），并添加`manage_team()`方法。\n\n**提示：**\n- 使用`super()`调用父类方法获取基础奖金\n- 注意方法重写时的参数传递\n- 子类可以添加父类没有的新方法\n\n**参考答案：**\n```python\nclass Employee:\n    def __init__(self, name, salary):\n        self.name = name\n        self.salary = salary\n    \n    def calculate_bonus(self):\n        return self.salary * 0.10\n    \n    def get_info(self):\n        return f\"员工: {self.name}，月薪: ${self.salary}\"\n\nclass Manager(Employee):\n    def __init__(self, name, salary, team_size):\n        super().__init__(name, salary)\n        self.team_size = team_size\n    \n    def calculate_bonus(self):\n        base_bonus = super().calculate_bonus()\n        manager_bonus = self.salary * 0.20 + 1000\n        return base_bonus + manager_bonus\n    \n    def manage_team(self):\n        return f\"{self.name}正在管理{self.team_size}人的团队\"\n    \n    def get_info(self):\n        base_info = super().get_info()\n        return f\"{base_info}，团队规模: {self.team_size}人\"\n\n# 测试\nemp = Employee(\"张三\", 5000)\nmgr = Manager(\"李四\", 8000, 5)\n\nprint(emp.get_info())\nprint(f\"员工奖金: ${emp.calculate_bonus()}\")\nprint()\n\nprint(mgr.get_info())\nprint(f\"经理奖金: ${mgr.calculate_bonus()}\")\nprint(mgr.manage_team())\n```\n\n---\n\n## 题目3：多重继承实践\n**难度：** ⭐⭐⭐⭐\n**要求：** 创建三个类：`Printer`（打印功能）、`Scanner`（扫描功能）、`Fax`（传真功能）。然后创建`MultifunctionPrinter`类继承这三个类，实现多功能打印机。\n\n**提示：**\n- 注意多重继承的顺序会影响方法解析\n- 每个父类应有独立的初始化方法\n- 子类需要调用所有父类的初始化方法\n\n**参考答案：**\n```python\nclass Printer:\n    def __init__(self):\n        self.print_quality = \"high\"\n    \n    def print_document(self, document):\n        return f\"打印文档: {document}，质量: {self.print_quality}\"\n    \n    def set_quality(self, quality):\n        self.print_quality = quality\n        return f\"打印质量设置为: {quality}\"\n\nclass Scanner:\n    def __init__(self):\n        self.scan_resolution = \"300dpi\"\n    \n    def scan_document(self, document):\n        return f\"扫描文档: {document}，分辨率: {self.scan_resolution}\"\n    \n    def set_resolution(self, resolution):\n        self.scan_resolution = resolution\n        return f\"扫描分辨率设置为: {resolution}\"\n\nclass Fax:\n    def __init__(self):\n        self.fax_number = None\n    \n    def send_fax(self, document, number):\n        self.fax_number = number\n        return f\"发送传真: {document} 到 {number}\"\n    \n    def receive_fax(self):\n        return f\"收到来自 {self.fax_number} 的传真\"\n\nclass MultifunctionPrinter(Printer, Scanner, Fax):\n    def __init__(self, model):\n        Printer.__init__(self)\n        Scanner.__init__(self)\n        Fax.__init__(self)\n        self.model = model\n        self.power_on = False\n    \n    def power_on_device(self):\n        self.power_on = True\n        return f\"{self.model} 多功能打印机已启动\"\n    \n    def power_off_device(self):\n        self.power_on = False\n        return f\"{self.model} 多功能打印机已关闭\"\n    \n    def device_status(self):\n        status = \"运行中\" if self.power_on else \"已关闭\"\n        return f\"\"\"\n设备型号: {self.model}\n设备状态: {status}\n打印质量: {self.print_quality}\n扫描分辨率: {self.scan_resolution}\n传真号码: {self.fax_number or '未设置'}\n        \"\"\".strip()\n\n# 测试\nmfp = MultifunctionPrinter(\"HP OfficeJet Pro\")\nprint(mfp.power_on_device())\nprint()\n\nprint(mfp.print_document(\"季度报告.pdf\"))\nprint(mfp.set_quality(\"draft\"))\nprint()\n\nprint(mfp.scan_document(\"合同.jpg\"))\nprint(mfp.set_resolution(\"600dpi\"))\nprint()\n\nprint(mfp.send_fax(\"报价单.doc\", \"021-12345678\"))\nprint(mfp.receive_fax())\nprint()\n\nprint(\"设备状态:\")\nprint(mfp.device_status())\n```\n\n---\n\n## 题目4：抽象基类设计\n**难度：** ⭐⭐⭐⭐⭐\n**要求：** 使用`abc`模块创建`BankAccount`抽象基类，要求子类必须实现`deposit()`、`withdraw()`、`get_balance()`方法。创建`SavingsAccount`（储蓄账户）和`CheckingAccount`（支票账户）子类，分别实现不同的取款规则。\n\n**提示：**\n- 使用`@abstractmethod`装饰器定义抽象方法\n- 储蓄账户取款不能超过余额，支票账户可以有透支额度\n- 注意异常处理和业务逻辑\n\n**参考答案：**\n```python\nfrom abc import ABC, abstractmethod\n\nclass BankAccount(ABC):\n    def __init__(self, account_number, account_holder, initial_balance=0):\n        self.account_number = account_number\n        self.account_holder = account_holder\n        self._balance = initial_balance\n        self.transactions = []\n    \n    @abstractmethod\n    def deposit(self, amount):\n        pass\n    \n    @abstractmethod\n    def withdraw(self, amount):\n        pass\n    \n    @abstractmethod\n    def get_balance(self):\n        pass\n    \n    def _record_transaction(self, type_, amount):\n        self.transactions.append({\n            'type': type_,\n            'amount': amount,\n            'balance': self._balance\n        })\n    \n    def get_transaction_history(self):\n        return self.transactions\n    \n    def get_account_info(self):\n        return f\"账户: {self.account_number}，户主: {self.account_holder}\"\n\nclass SavingsAccount(BankAccount):\n    def __init__(self, account_number, account_holder, initial_balance=0):\n        super().__init__(account_number, account_holder, initial_balance)\n        self.account_type = \"储蓄账户\"\n    \n    def deposit(self, amount):\n        if amount <= 0:\n            return \"存款金额必须大于0\"\n        \n        self._balance += amount\n        self._record_transaction('存款', amount)\n        return f\"存款 ${amount} 成功，当前余额: ${self._balance}\"\n    \n    def withdraw(self, amount):\n        if amount <= 0:\n            return \"取款金额必须大于0\"\n        \n        if amount > self._balance:\n            return f\"取款失败！余额不足。当前余额: ${self._balance}\"\n        \n        self._balance -= amount\n        self._record_transaction('取款', amount)\n        return f\"取款 ${amount} 成功，当前余额: ${self._balance}\"\n    \n    def get_balance(self):\n        return self._balance\n    \n    def calculate_interest(self, rate):\n        interest = self._balance * rate / 100\n        self._balance += interest\n        self._record_transaction('利息', interest)\n        return f\"利息收入: ${interest:.2f}，当前余额: ${self._balance:.2f}\"\n\nclass CheckingAccount(BankAccount):\n    def __init__(self, account_number, account_holder, initial_balance=0, overdraft_limit=500):\n        super().__init__(account_number, account_holder, initial_balance)\n        self.account_type = \"支票账户\"\n        self.overdraft_limit = overdraft_limit\n    \n    def deposit(self, amount):\n        if amount <= 0:\n            return \"存款金额必须大于0\"\n        \n        self._balance += amount\n        self._record_transaction('存款', amount)\n        return f\"存款 ${amount} 成功，当前余额: ${self._balance}\"\n    \n    def withdraw(self, amount):\n        if amount <= 0:\n            return \"取款金额必须大于0\"\n        \n        available_balance = self._balance + self.overdraft_limit\n        \n        if amount > available_balance:\n            return f\"取款失败！超出透支额度。可用余额: ${available_balance}\"\n        \n        self._balance -= amount\n        self._record_transaction('取款', amount)\n        \n        if self._balance < 0:\n            return f\"取款 ${amount} 成功（已使用透支），当前余额: ${self._balance}\"\n        return f\"取款 ${amount} 成功，当前余额: ${self._balance}\"\n    \n    def get_balance(self):\n        return self._balance\n    \n    def get_available_balance(self):\n        return self._balance + self.overdraft_limit\n\n# 测试\nprint(\"=== 储蓄账户测试 ===\")\nsavings = SavingsAccount(\"SAV001\", \"张三\", 1000)\nprint(savings.get_account_info())\nprint(savings.deposit(500))\nprint(savings.withdraw(200))\nprint(savings.withdraw(2000))  # 应该失败\nprint(savings.calculate_interest(2.5))\nprint(f\"当前余额: ${savings.get_balance()}\")\nprint()\n\nprint(\"=== 支票账户测试 ===\")\nchecking = CheckingAccount(\"CHK001\", \"李四\", 500, 1000)\nprint(checking.get_account_info())\nprint(checking.deposit(300))\nprint(checking.withdraw(700))\nprint(checking.withdraw(1200))  # 使用透支\nprint(checking.withdraw(500))   # 应该失败\nprint(f\"当前余额: ${checking.get_balance()}\")\nprint(f\"可用余额（含透支）: ${checking.get_available_balance()}\")\nprint()\n\n# 测试抽象基类约束\ntry:\n    class InvalidAccount(BankAccount):\n        pass\n    \n    invalid = InvalidAccount(\"TEST\", \"测试\")\nexcept TypeError as e:\n    print(\"抽象基类约束测试:\")\n    print(f\"✓ 错误信息: {e}\")\n```\n\n---\n\n## 题目5：综合应用 - 电商系统设计\n**难度：** ⭐⭐⭐⭐⭐\n**要求：** 设计一个电商系统的产品类层次结构。创建`Product`基类，然后创建`PhysicalProduct`（实物商品）和`DigitalProduct`（数字商品）子类。再进一步创建`Book`（继承`PhysicalProduct`）和`Ebook`（继承`DigitalProduct`）类，实现不同的价格计算和配送方式。\n\n**提示：**\n- 考虑使用多层继承\n- 注意方法重写和扩展\n- 实现多态性：统一接口处理不同类型产品\n\n**参考答案：**\n```python\nclass Product:\n    def __init__(self, name, base_price):\n        self.name = name\n        self.base_price = base_price\n        self.sku = self._generate_sku()\n    \n    def _generate_sku(self):\n        import uuid\n        return str(uuid.uuid4())[:8].upper()\n    \n    def calculate_price(self, quantity=1):\n        return self.base_price * quantity\n    \n    def get_product_info(self):\n        return f\"产品: {self.name} | SKU: {self.sku} | 单价: ${self.base_price}\"\n    \n    def apply_discount(self, discount_percent):\n        if 0 <= discount_percent <= 100:\n            return self.base_price * (1 - discount_percent / 100)\n        return self.base_price\n\nclass PhysicalProduct(Product):\n    def __init__(self, name, base_price, weight, dimensions):\n        super().__init__(name, base_price)\n        self.weight = weight  # 重量（kg）\n        self.dimensions = dimensions  # 尺寸（长x宽x高）\n        self.product_type = \"实物商品\"\n    \n    def calculate_shipping(self, distance):\n        \"\"\"计算运费\"\"\"\n        base_shipping = 5.0\n        weight_cost = self.weight * 2.0\n        distance_cost = distance * 0.1\n        return base_shipping + weight_cost + distance_cost\n    \n    def calculate_price(self, quantity=1, distance=0):\n        \"\"\"计算总价（含运费）\"\"\"\n        product_cost = super().calculate_price(quantity)\n        shipping_cost = self.calculate_shipping(distance) * quantity\n        return product_cost + shipping_cost\n    \n    def get_product_info(self):\n        base_info = super().get_product_info()\n        return f\"{base_info} | 类型: {self.product_type} | 重量: {self.weight}kg\"\n\nclass DigitalProduct(Product):\n    def __init__(self, name, base_price, file_size, download_limit):\n        super().__init__(name, base_price)\n        self.file_size = file_size  # 文件大小（MB）\n        self.download_limit = download_limit  # 下载次数限制\n        self.product_type = \"数字商品\"\n    \n    def calculate_price(self, quantity=1):\n        \"\"\"数字商品没有运费\"\"\"\n        return super().calculate_price(quantity)\n    \n    def deliver(self, email):\n        \"\"\"数字商品配送方式\"\"\"\n        return f\"下载链接已发送到 {email}，文件大小: {self.file_size}MB，剩余下载次数: {self.download_limit}\"\n    \n    def get_product_info(self):\n        base_info = super().get_product_info()\n        return f\"{base_info} | 类型: {self.product_type} | 文件大小: {self.file_size}MB\"\n\nclass Book(PhysicalProduct):\n    def __init__(self, name, base_price, weight, dimensions, author, pages):\n        super().__init__(name, base_price, weight, dimensions)\n        self.author = author\n        self.pages = pages\n        self.category = \"图书\"\n    \n    def calculate_price(self, quantity=1, distance=0):\n        \"\"\"图书有特殊折扣规则\"\"\"\n        base_price = super().calculate_price(quantity, distance)\n        # 图书超过3本打9折\n        if quantity >= 3:\n            base_price *= 0.9\n        return round(base_price, 2)\n    \n    def get_product_info(self):\n        base_info = super().get_product_info()\n        return f\"{base_info} | 作者: {self.author} | 页数: {self.pages} | 分类: {self.category}\"\n\nclass Ebook(DigitalProduct):\n    def __init__(self, name, base_price, file_size, download_limit, format_type):\n        super().__init__(name, base_price, file_size, download_limit)\n        self.format_type = format_type\n        self.category = \"电子书\"\n    \n    def calculate_price(self, quantity=1):\n        \"\"\"电子书买二送一\"\"\"\n        actual_quantity = quantity + (quantity // 2)\n        return super().calculate_price(actual_quantity)\n    \n    def deliver(self, email):\n        base_delivery = super().deliver(email)\n        return f\"{base_delivery} | 格式: {self.format_type}\"\n    \n    def get_product_info(self):\n        base_info = super().get_product_info()\n        return f\"{base_info} | 格式: {self.format_type} | 分类: {self.category}\"\n\n# 测试\nprint(\"=== 电商系统产品测试 ===\")\nprint()\n\n# 创建各种产品\nproducts = [\n    PhysicalProduct(\"T恤\", 25.0, 0.3, \"30x40x2\"),\n    DigitalProduct(\"软件许可证\", 99.0, 150, 3),\n    Book(\"Python编程\", 45.0, 0.8, \"20x15x3\", \"John Doe\", 350),\n    Ebook(\"Python电子书\", 35.0, 10, 5, \"PDF\"),\n]\n\n# 显示产品信息\nprint(\"1. 产品信息:\")\nfor product in products:\n    print(f\"   {product.get_product_info()}\")\nprint()\n\n# 测试价格计算\nprint(\"2. 价格计算:\")\ntest_cases = [\n    (products[0], 2, 100),  # T恤，2件，100公里\n    (products[1], 1, 0),    # 软件，1个\n    (products[2], 4, 50),   # 图书，4本，50公里（应该打折）\n    (products[3], 3, 0),    # 电子书，3本（买二送一）\n]\n\nfor product, quantity, distance in test_cases:\n    if isinstance(product, PhysicalProduct):\n        price = product.calculate_price(quantity, distance)\n        print(f\"   {product.name} x{quantity}，距离{distance}km: ${price:.2f}\")\n    else:\n        price = product.calculate_price(quantity)\n        print(f\"   {product.name} x{quantity}: ${price:.2f}\")\nprint()\n\n# 测试配送方式\nprint(\"3. 配送方式:\")\nfor product in products:\n    if isinstance(product, DigitalProduct):\n        print(f\"   {product.name}: {product.deliver('customer@example.com')}\")\n    elif isinstance(product, PhysicalProduct):\n        print(f\"   {product.name}: 需要物流配送，运费: ${product.calculate_shipping(50):.2f}（50公里）\")\nprint()\n\n# 多态性演示\nprint(\"4. 多态性演示 - 统一处理所有产品:\")\ntotal_value = 0\nfor product in products:\n    if isinstance(product, PhysicalProduct):\n        total_value += product.calculate_price(2, 100)\n    else:\n        total_value += product.calculate_price(2)\n\nprint(f\"   所有产品各买2件的总价值: ${total_value:.2f}\")\nprint()\n\n# 类型检查\nprint(\"5. 继承关系检查:\")\nebook = products[3]\nprint(f\"   Ebook是DigitalProduct的实例吗？ {isinstance(ebook, DigitalProduct)}\")\nprint(f\"   Ebook是Product的实例吗？ {isinstance(ebook, Product)}\")\nprint(f\"   Ebook是PhysicalProduct的实例吗？ {isinstance(ebook, PhysicalProduct)}\")\n```",
      "related_knowledge": [],
      "keywords": [
        "Python",
        "类",
        "对象"
      ],
      "difficulty": "basic",
      "type": "general",
      "user_level": "intermediate",
      "creation_time": "2026-03-17T18:27:26.781097",
      "last_accessed": "2026-03-17T18:27:26.781097",
      "access_count": 1,
      "importance_score": 0.0,
      "mastery_score": 0.5,
      "review_priority": "normal",
      "created_at": "2026-03-17T18:27:26.781612",
      "updated_at": "2026-03-17T18:27:26.781612",
      "status": "learning",
      "proficiency": 0
    },
    "be5a85e831dc9b0d48370f4104c7d5c2": {
      "id": "be5a85e831dc9b0d48370f4104c7d5c2",
      "title": "什么是Python的继承？",
      "content": "# Python继承：像家族传承一样的代码复用\n\n## 一、生活化类比：家族传承\n\n想象一下你出生在一个音乐世家：\n- 你的父母（父类）都是音乐家，他们天生就会识谱、弹奏乐器\n- 你（子类）一出生就**继承**了这些音乐天赋，不需要重新学习\n- 但你还可以发展自己的特长，比如父母会钢琴，你除了会钢琴，还学会了吉他\n- 甚至你可以选择不继承某些特点，比如父母喜欢古典音乐，你却喜欢摇滚\n\n这就是继承的...",
      "topics": [
        "类"
      ],
      "example": "```python\n\"\"\"\n示例1：基础继承演示\n难度：初级\n说明：展示最基本的继承语法，子类如何自动获得父类的方法\n\"\"\"\n\n# 父类：动物\nclass Animal:\n    def __init__(self, name):\n        self.name = name\n    \n    def eat(self):\n        print(f\"{self.name}正在吃东西\")\n    \n    def sleep(self):\n        print(f\"{self.name}正在睡觉\")\n\n# 子类：狗，继承自动物类\nclass Dog(Animal):\n    def bark(self):\n        print(f\"{self.name}在汪汪叫\")\n\n# 创建Dog类的实例\nmy_dog = Dog(\"旺财\")\nprint(\"=== 基础继承演示 ===\")\nmy_dog.eat()    # 调用继承自Animal类的方法\nmy_dog.sleep()  # 调用继承自Animal类的方法\nmy_dog.bark()   # 调用Dog类自己的方法\n\nprint(\"\\n\" + \"=\"*50 + \"\\n\")\n\n\"\"\"\n示例2：方法重写和super()使用\n难度：中级\n说明：展示如何重写父类方法，以及使用super()调用父类方法\n\"\"\"\n\n# 父类：交通工具\nclass Vehicle:\n    def __init__(self, brand, speed):\n        self.brand = brand\n        self.speed = speed\n    \n    def move(self):\n        print(f\"{self.brand}正在移动\")\n    \n    def show_info(self):\n        print(f\"品牌：{self.brand}，速度：{self.speed}km/h\")\n\n# 子类：汽车，重写move方法\nclass Car(Vehicle):\n    def __init__(self, brand, speed, fuel_type):\n        # 使用super()调用父类的__init__方法\n        super().__init__(brand, speed)\n        self.fuel_type = fuel_type\n    \n    # 重写父类的move方法\n    def move(self):\n        print(f\"{self.brand}汽车在路上行驶\")\n    \n    # 扩展父类的show_info方法\n    def show_info(self):\n        # 先调用父类的方法\n        super().show_info()\n        # 再添加子类特有的信息\n        print(f\"燃料类型：{self.fuel_type}\")\n\n# 子类：飞机，完全重写move方法\nclass Airplane(Vehicle):\n    def move(self):\n        print(f\"{self.brand}飞机在空中飞行\")\n\nprint(\"=== 方法重写和super()演示 ===\")\n# 创建Car实例\nmy_car = Car(\"丰田\", 120, \"汽油\")\nmy_car.show_info()  # 调用重写后的方法\nmy_car.move()       # 调用重写后的方法\n\n# 创建Airplane实例\nmy_plane = Airplane(\"波音\", 900)\nmy_plane.move()     # 调用完全重写的方法\n\nprint(\"\\n\" + \"=\"*50 + \"\\n\")\n\n\"\"\"\n示例3：多态和继承层次结构\n难度：中级\n说明：展示继承如何实现多态，以及创建类层次结构\n\"\"\"\n\n# 基类：员工\nclass Employee:\n    def __init__(self, name, employee_id):\n        self.name = name\n        self.employee_id = employee_id\n    \n    def work(self):\n        print(f\"{self.name}正在工作\")\n    \n    def get_salary(self):\n        return 5000  # 基础工资\n\n# 子类：经理\nclass Manager(Employee):\n    def __init__(self, name, employee_id, department):\n        super().__init__(name, employee_id)\n        self.department = department\n    \n    # 重写work方法\n    def work(self):\n        print(f\"{self.name}经理正在管理{self.department}部门\")\n    \n    # 重写get_salary方法\n    def get_salary(self):\n        base_salary = super().get_salary()\n        return base_salary + 3000  # 经理有额外津贴\n\n# 子类：开发人员\nclass Developer(Employee):\n    def __init__(self, name, employee_id, programming_language):\n        super().__init__(name, employee_id)\n        self.programming_language = programming_language\n    \n    # 重写work方法\n    def work(self):\n        print(f\"{self.name}正在用{self.programming_language}写代码\")\n    \n    # 重写get_salary方法\n    def get_salary(self):\n        base_salary = super().get_salary()\n        return base_salary + 2000  # 开发人员有技术津贴\n\nprint(\"=== 多态和继承层次演示 ===\")\n# 创建不同类型的员工\nemployees = [\n    Manager(\"张三\", \"M001\", \"技术部\"),\n    Developer(\"李四\", \"D001\", \"Python\"),\n    Developer(\"王五\", \"D002\", \"Java\"),\n    Employee(\"赵六\", \"E001\")  # 普通员工\n]\n\n# 多态演示：统一处理不同类型的对象\nfor emp in employees:\n    emp.work()  # 每个员工有自己的工作方式\n    salary = emp.get_salary()\n    print(f\"{emp.name}的工资是：{salary}元\")\n    print(\"-\" * 30)\n\nprint(\"\\n\" + \"=\"*50 + \"\\n\")\n\n\"\"\"\n示例4：多重继承（谨慎使用）\n难度：中级\n说明：展示Python的多重继承特性\n\"\"\"\n\nclass Camera:\n    \"\"\"相机类\"\"\"\n    def take_photo(self):\n        print(\"拍照功能：咔嚓！拍了一张照片\")\n    \n    def zoom(self):\n        print(\"相机变焦：放大画面\")\n\nclass Phone:\n    \"\"\"手机类\"\"\"\n    def make_call(self, number):\n        print(f\"打电话：正在拨打{number}\")\n    \n    def send_message(self, message):\n        print(f\"发短信：{message}\")\n\nclass MusicPlayer:\n    \"\"\"音乐播放器类\"\"\"\n    def play_music(self, song):\n        print(f\"播放音乐：正在播放《{song}》\")\n    \n    def pause(self):\n        print(\"音乐播放：已暂停\")\n\n# 智能手机类，继承多个父类\nclass SmartPhone(Camera, Phone, MusicPlayer):\n    \"\"\"智能手机类，具有相机、手机、音乐播放器的功能\"\"\"\n    def __init__(self, brand):\n        self.brand = brand\n    \n    def show_features(self):\n        print(f\"\\n{self.brand}智能手机功能演示：\")\n\nprint(\"=== 多重继承演示 ===\")\n# 创建智能手机实例\nmy_phone = SmartPhone(\"华为\")\nmy_phone.show_features()\n\n# 调用来自不同父类的方法\nmy_phone.take_photo()          # 来自Camera类\nmy_phone.make_call(\"13800138000\")  # 来自Phone类\nmy_phone.play_music(\"小星星\")   # 来自MusicPlayer类\n\n# 查看方法解析顺序（MRO）\nprint(f\"\\n方法解析顺序：{SmartPhone.__mro__}\")\n\n# 运行所有示例\nif __name__ == \"__main__\":\n    # 示例已经在上面直接运行了\n    print(\"\\n所有示例运行完成！\")\n```",
      "practice": "# Python继承练习题\n\n## 题目一：基础继承实现（难度：初级）\n\n创建一个`Shape`（形状）基类，包含以下内容：\n1. 初始化方法接收`name`（形状名称）和`color`（颜色）参数\n2. 一个`describe()`方法，返回字符串\"这是一个{color}色的{name}\"\n\n创建两个子类：\n1. `Circle`（圆形）类，继承自`Shape`\n   - 添加`radius`（半径）属性\n   - 重写`describe()`方法，返回\"这是一个{color}色的圆形，半径为{radius}\"\n   - 添加`area()`方法，计算并返回圆形的面积（π×半径²，π取3.14）\n\n2. `Rectangle`（矩形）类，继承自`Shape`\n   - 添加`width`（宽度）和`height`（高度）属性\n   - 重写`describe()`方法，返回\"这是一个{color}色的矩形，宽{width}，高{height}\"\n   - 添加`area()`方法，计算并返回矩形的面积\n\n**要求：**\n- 使用`super()`调用父类的初始化方法\n- 创建实例并测试所有方法\n\n**提示：**\n- 子类的`__init__`方法需要先调用父类的`__init__`\n- 记得在重写的方法中使用`self`访问属性\n\n---\n\n## 题目二：多态应用（难度：中级）\n\n设计一个简单的支付系统，包含以下类：\n\n1. `PaymentMethod`（支付方式）基类：\n   - 初始化方法接收`account_holder`（账户持有人）参数\n   - 抽象方法`process_payment(amount)`，处理支付\n   - 方法`get_info()`，返回支付方式基本信息\n\n2. `CreditCard`（信用卡）类，继承自`PaymentMethod`：\n   - 添加`card_number`（卡号）和`expiry_date`（有效期）属性\n   - 实现`process_payment(amount)`方法，返回\"信用卡支付成功：从卡号{card_number}扣款{amount}元\"\n   - 重写`get_info()`方法，返回\"信用卡持有人：{account_holder}\"\n\n3. `PayPal`（贝宝）类，继承自`PaymentMethod`：\n   - 添加`email`（邮箱）属性\n   - 实现`process_payment(amount)`方法，返回\"PayPal支付成功：从邮箱{email}扣款{amount}元\"\n   - 重写`get_info()`方法，返回\"PayPal账户：{email}\"\n\n4. `BankTransfer`（银行转账）类，继承自`PaymentMethod`：\n   - 添加`bank_name`（银行名称）和`account_number`（账号）属性\n   - 实现`process_payment(amount)`方法，返回\"银行转账成功：从{account_holder}的{bank_name}账户转账{amount}元\"\n   - 重写`get_info()`方法，返回\"银行账户：{bank_name} - {account_number}\"\n\n**要求：**\n1. 创建不同类型的支付方式实例\n2. 使用多态：将所有支付方式对象放入一个列表，循环调用它们的`process_payment()`方法\n3. 模拟处理一笔100元的支付\n\n**提示：**\n- 注意每个子类需要调用父类的`__init__`方法\n- 多态允许不同类型的对象以统一的方式处理\n\n---\n\n## 题目三：继承层次设计（难度：中级）\n\n设计一个图书馆管理系统中的资料类层次结构：\n\n1. `LibraryItem`（图书馆资料）基类：\n   - 属性：`title`（标题）、`item_id`（编号）、`is_checked_out`（是否借出）\n   - 方法：`check_out()`（借出）、`return_item()`（归还）、`get_info()`\n\n2. `Book`（图书）类，继承自`LibraryItem`：\n   - 添加属性：`author`（作者）、`isbn`（ISBN号）、`pages`（页数）\n   - 重写`get_info()`方法，返回完整的图书信息\n\n3. `Magazine`（杂志）类，继承自`LibraryItem`：\n   - 添加属性：`issue_number`（期号）、`publication_date`（出版日期）\n   - 重写`get_info()`方法，返回杂志信息\n\n4. `DVD`（光盘）类，继承自`LibraryItem`：\n   - 添加属性：`director`（导演）、`duration`（时长，分钟）、`genre`（类型）\n   - 重写`get_info()`方法，返回DVD信息\n   - 添加特有方法：`play_trailer()`，返回\"正在播放{title}的预告片\"\n\n**要求：**\n1. 实现完整的类定义，包括所有方法和属性\n2. 创建每种资料的实例并测试\n3. 实现`check_out()`和`return_item()`方法，改变`is_checked_out`状态并给出相应提示\n\n**扩展挑战：**\n- 添加`Reservable`（可预订）混合类，包含`reserve()`方法\n- 创建`ReservableBook`类，多重继承自`Book`和`Reservable`\n\n---\n\n## 题目四：综合应用（难度：中高级）\n\n设计一个电商系统的商品类结构：\n\n1. `Product`（商品）基类：\n   - 属性：`name`（名称）、`price`（价格）、`sku`（库存编码）\n   - 方法：`display_info()`、`calculate_discount(discount_rate)`（计算折扣价）\n\n2. `PhysicalProduct`（实体商品）类，继承自`Product`：\n   - 添加属性：`weight`（重量，kg）、`dimensions`（尺寸）\n   - 添加方法：`calculate_shipping()`（计算运费，基础运费10元 + 重量×2元）\n\n3. `DigitalProduct`（数字商品）类，继承自`Product`：\n   - 添加属性：`file_size`（文件大小，MB）、`download_link`（下载链接）\n   - 重写`calculate_discount()`，数字商品最低打8折\n\n4. `SubscriptionProduct`（订阅商品）类，继承自`Product`：\n   - 添加属性：`duration_months`（订阅时长，月）、`auto_renew`（是否自动续订）\n   - 添加方法：`calculate_yearly_price()`（计算年费）\n\n**要求：**\n1. 实现完整的继承层次\n2. 使用`super()`适当调用父类方法\n3. 创建每种商品的实例并测试所有方法\n4. 实现一个`ShoppingCart`类，可以添加不同类型的商品并计算总价\n\n**提示：**\n- 注意方法重写时保持一致性\n- 考虑使用类属性或类方法实现共享功能\n\n---\n\n## 参考答案要点\n\n### 题目一要点：\n```python\nclass Shape:\n    def __init__(self, name, color):\n        self.name = name\n        self.color = color\n    \n    def describe(self):\n        return f\"这是一个{self.color}色的{self.name}\"\n\nclass Circle(Shape):\n    def __init__(self, color, radius):\n        super().__init__(\"圆形\", color)\n        self.radius = radius\n    \n    def describe(self):\n        return f\"这是一个{self.color}色的圆形，半径为{self.radius}\"\n    \n    def area(self):\n        return 3.14 * self.radius ** 2\n```\n\n### 题目二要点：\n```python\nclass PaymentMethod:\n    def __init__(self, account_holder):\n        self.account_holder = account_holder\n    \n    def process_payment(self, amount):\n        raise NotImplementedError(\"子类必须实现此方法\")\n    \n    def get_info(self):\n        return f\"支付方式持有人：{self.account_holder}\"\n\n# 多态使用示例\npayments = [CreditCard(...), PayPal(...), BankTransfer(...)]\nfor payment in payments:\n    print(payment.process_payment(100))\n```\n\n### 题目三要点：\n- 基类中实现状态管理（借出/归还）\n- 子类重写`get_info()`提供详细信息\n- 多重继承时注意方法解析顺序\n\n### 题目四要点：\n- 合理设计继承层次，体现\"是一个\"关系\n- 实体商品和数字商品有不同的业务逻辑\n- 购物车需要处理不同类型的商品",
      "related_knowledge": [],
      "keywords": [
        "Python",
        "类",
        "对象"
      ],
      "difficulty": "basic",
      "type": "concept",
      "user_level": "intermediate",
      "creation_time": "2026-03-17T19:04:07.154656",
      "last_accessed": "2026-03-17T19:04:07.154656",
      "access_count": 1,
      "importance_score": 0.0,
      "mastery_score": 0.5,
      "review_priority": "normal",
      "created_at": "2026-03-17T19:04:07.156222",
      "updated_at": "2026-03-17T19:04:07.156222",
      "status": "learning",
      "proficiency": 0
    }
  },
  "relationships": [
    {
      "id": "65e56c7a-538d-4835-ae19-fe329e3b7ff9",
      "source_id": "kp-decorator",
      "target_id": "kp-singleton",
      "type": "prerequisite",
      "weight": 0.8,
      "created_at": "2026-03-17T17:08:51.723950"
    },
    {
      "id": "1f11d805-c75e-4de0-8860-49abfc6560ee",
      "source_id": "kp-metaclass",
      "target_id": "kp-singleton",
      "type": "prerequisite",
      "weight": 0.6,
      "created_at": "2026-03-17T17:08:51.725486"
    }
  ]
}